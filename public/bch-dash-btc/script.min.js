function findGetParameter(parameterName) {
    var result = null,
        tmp = [];
    location
        .search
        .substr(1)
        .split("&")
        .forEach(function (item) {
            tmp = item.split("=");
            if (tmp[0] === parameterName)
                result = decodeURIComponent(tmp[1]);
            }
        );
    return result;
}

function showLoading() {
    document.getElementById("list").innerHTML="";
    document.getElementById("loadingMainContainer").innerHTML="<div id='loadingMain'></div>";

    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading...</i></h3>";

    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading....</i></h3>";}
    , 500);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3>&nbsp;Loading.....</h3>";}
    , 750);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading......</i></h3>";}
    , 1075);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading.......</i></h3>";}
    , 1600);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading........</i></h3>";}
    , 2400);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading.........</i></h3>";}
    , 3600);
    setTimeout(function () {
    document.getElementById("loadingMain").innerHTML="<h3><i>&nbsp;Loading..........</i></h3>";}
    , 5400);
}

function hideLoading() {
    document.getElementById("loadingMainContainer").innerHTML="";
}

function loadMore() {
    var xmlhttp = new XMLHttpRequest();
    showLoading();

    xmlhttp.onreadystatechange = function () {
        if (xmlhttp.readyState == XMLHttpRequest.DONE) {
            if (xmlhttp.status == 200) {
                hideLoading();
                document
                    .getElementById("list")
                    .innerHTML += xmlhttp.responseText;

                new LazyLoad({elements_selector: ".lazy"});
            } else if (xmlhttp.status == 400) {
                alert('There was an error 400');
            } else {
                alert('something else other than 200 was returned');
            }
        }
    };
    //var param = findGetParameter("filter");
    xmlhttp.open("GET", "listcontent.php" + location.search, true);
    xmlhttp.send();
}